import { TruncateTooltipPipe } from './truncate-tooltip.pipe';

describe('TruncateTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
