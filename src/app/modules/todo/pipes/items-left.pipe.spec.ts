import { ItemsLeftPipe } from './items-left.pipe';

describe('ItemsLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemsLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
